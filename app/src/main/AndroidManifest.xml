<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zuoo.projektpola">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Zliczanie pól figur"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Activity0">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".RectangleActivity"
            android:label="Pole prostokąta"></activity>


        <activity
            android:name=".TriangleActivity"
            android:label="Pole trójkąta"></activity>

        <activity
            android:name=".CircleActivity"
            android:label="Pole koła"></activity>

    </application>

</manifest>